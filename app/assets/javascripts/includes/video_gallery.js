// JavaScript Document

$(document).ready(function(){
	
	$('a.videoLink').each(function(){
		
		var thumbnailFilePath = 'video/' + $(this).attr('videofile') + '.jpg';  // this will fetch the image file from folder by using the same video name + .jpg
  		
		var videoCaption = $(this).attr('videocaption');
		
		})
	
	
	});